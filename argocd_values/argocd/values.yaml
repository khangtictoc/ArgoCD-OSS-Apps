# Reference: https://artifacthub.io/packages/helm/argo/argo-cd?modal=values

server:
  replicas: 1
  metrics:
    enabled: true
  ingress:
    enabled: false
    labels: {}
    annotations: {}
    ingressClassName: ""
    hostname: ""
    path: /
    tls: false

configs:
  params:
    application.namespaces: "argocd-apps" # "*" for wildcard, namespaces are separated by commas
  repositories:
    argocd-apps:
      url: https://github.com/khangtictoc/ArgoCD-Apps.git
      type: git
      name: ArgoCD-Apps
  # cm:
  #   resource.exclusions: ""

controller:
  replicas: 1
  metrics:
    enabled: true

repoServer:
  replicas: 1
  metrics:
    enabled: true

applicationSet:
  replicas: 1
  metrics:
    enabled: true

redis:
  replicas: 1
  metrics:
    enabled: true

notifications:
  enabled: true
  replicas: 1
  argocdUrl: ""
  metrics:
    enabled: true

commitServer:
  enabled: true
  replicas: 1
  metrics:
    enabled: true
